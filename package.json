{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "build:production": "npm ci && npm run build",
        "build:netlify": "npm ci && npm run build",
        "build:railway": "npm ci && npm run build",
        "build:heroku": "npm ci && npm run build"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.4"
    },
    "dependencies": {
        "flowbite": "^3.1.2",
        "puppeteer": "^24.30.0"
    }
}
